# Resume_classification
Built a machine learning model to automatically classify resumes into predefined job categories using NLP techniques. Implemented text preprocessing, feature extraction (TF-IDF), and trained multiple classifiers to improve accuracy.
